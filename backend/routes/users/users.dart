import 'dart:io';

import 'package:dart_frog/dart_frog.dart';

/// Placeholder users endpoint — not yet implemented.
Future<Response> onRequest(RequestContext context) async {
  return Response(statusCode: HttpStatus.notImplemented);
}
